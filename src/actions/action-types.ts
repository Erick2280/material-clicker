// TODO: Change this to the item interface and SciNum data structure.
// Ignore this linting error, it will stop complaining about it once we add multiple actions.
export type ItemsActionObject = { type: 'ADD_ITEM', item: string, amount: number };

export type ActionObject = ItemsActionObject;
